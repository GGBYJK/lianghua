# 数据源入口：tqsdk / aliyun / tushare / infoway
MARKET_DATA_PROVIDER=tqsdk

# TqSdk 行情账号。c0 默认映射为 KQ.m@DCE.c。
TQ_ACCOUNT=你的快期账号
TQ_PASSWORD=你的快期密码
TQ_DEFAULT_EXCHANGE=DCE
TQ_TIMEOUT=30
TQ_FIRST_DATA_TIMEOUT=30
TQ_REQUEST_TIMEOUT=30
TQ_MIN_DATA_LENGTH=240

# Watch pool scanning
REQUEST_TIMEOUT_SECONDS=30
REQUEST_TIMEOUT_KILL_WORKER=1
GUNICORN_TIMEOUT=30
GUNICORN_GRACEFUL_TIMEOUT=30
GUNICORN_MAX_REQUESTS=1000
GUNICORN_MAX_REQUESTS_JITTER=200
WATCH_POOL_MONITOR_ENABLED=true
WATCH_POOL_MONITOR_IN_API=false
WATCH_POOL_KLINE_LIMIT=240
WATCH_POOL_SCAN_CONCURRENCY=3
TRADING_WORKER_POLL_SECONDS=2
TRADING_WORKER_LEASE_SECONDS=15
QUOTE_STALE_SECONDS=30
DEFAULT_SLIPPAGE_TICKS=1
MIN_DEFAULT_TAKE_PROFIT_RR=1.5
SIGNAL_TRADEABLE_HOURS=24

# Paper trading authentication and bootstrap account
JWT_SECRET=replace-with-a-long-random-secret
ACCESS_TOKEN_MINUTES=15
REFRESH_TOKEN_DAYS=7
COOKIE_SECURE=false
BOOTSTRAP_ADMIN_USERNAME=admin
BOOTSTRAP_ADMIN_PASSWORD=admin123456
DEFAULT_INITIAL_BALANCE=1000000

# 阿里云市场购买接口后，在控制台复制 K线接口请求地址和 AppCode。
ALIYUN_MARKET_KLINE_URL=https://future.market.alicloudapi.com/kline
ALIYUN_MARKET_APPCODE=你的AppCode

# 如果接口文档里的参数名不同，在这里改。
ALIYUN_MARKET_SYMBOL_PARAM=symbol
ALIYUN_MARKET_PERIOD_PARAM=type
ALIYUN_MARKET_LIMIT_PARAM=limit

# 如果接口要求额外固定参数，按 querystring 写。
# ALIYUN_MARKET_EXTRA_PARAMS=market=SHFE&type=kline

MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=123123
MYSQL_DATABASE=lh_demo

# Enterprise WeChat robot notification. Keep the real key in .env only.
WECHAT_WORKBOT_WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=your-key
WECHAT_WORKBOT_MENTIONED_LIST=
WECHAT_WORKBOT_TIMEOUT_SECONDS=8
